<%= form_for(@false_word) do |f| %>
  <% if @false_word.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@false_word.errors.count, "error") %> prohibited this false_word from being saved:</h2>

      <ul>
      <% @false_word.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :word_id %><br>
    <%= f.number_field :word_id %>
  </div>
  <div class="field">
    <%= f.label :word %><br>
    <%= f.text_area :word %>
  </div>
  <div class="field">
    <%= f.label :level %><br>
    <%= f.number_field :level %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
